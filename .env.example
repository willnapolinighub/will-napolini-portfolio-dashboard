# ── Main Supabase (Auth + Settings + fallback for all data) ───
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
# Service role key — bypasses RLS. NEVER expose to client. Server-side only.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ── Optional: Separate Supabase projects per data type ────────
# Leave empty to use the main DB above (fallback).
# Set these to isolate data across multiple Supabase projects.
POSTS_SUPABASE_URL=
POSTS_SERVICE_ROLE_KEY=

PRODUCTS_SUPABASE_URL=
PRODUCTS_SERVICE_ROLE_KEY=

SUBSCRIBERS_SUPABASE_URL=
SUBSCRIBERS_SERVICE_ROLE_KEY=

ANALYTICS_SUPABASE_URL=
ANALYTICS_SERVICE_ROLE_KEY=

# ── Admin API ─────────────────────────────────────────
# Secret key used by frontend apps to call /api/public
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_API_KEY=your-secret-api-key-here

# URL of your frontend app (used for CORS allow-list)
FRONTEND_URL=https://your-frontend.vercel.app

# ── Stripe ───────────────────────────────────────────
# Secret key for creating products & payment links server-side
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...

# Webhook secret for verifying Stripe events
# Get from: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_...

# ── AI Providers (server-side only) ──────────────────
# Optional: set here as fallbacks if not saved in Supabase settings
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# ── n8n Tool Calling ──────────────────────────────────
# Webhook that handles admin AI tools (create_post, create_product, delete_post, get_posts, get_products)
# Server-side only — never exposed to the browser
N8N_WEBHOOK_URL=http://localhost:5678/webhook/admin-tools

# ── Media ─────────────────────────────────────────────
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloud-name
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=your-upload-preset
NEXT_PUBLIC_GOOGLE_API_KEY=
NEXT_PUBLIC_GOOGLE_SEARCH_ENGINE_ID=
